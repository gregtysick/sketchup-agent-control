# Tests

Codex should add automated tests for command schemas, path restrictions, request IDs, atomic queue handling, capability detection, and unsupported-operation rejection.

Tests performed outside SketchUp must be reported separately from tests actually exercised inside SketchUp.