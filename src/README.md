# Extension Source

Codex should scaffold the SketchUp Ruby extension in this directory using official SketchUp extension structure and the namespace `BeautifulInsights::SketchUpAgentControl`.

Do not add model-changing commands until the read-only bridge is verified.